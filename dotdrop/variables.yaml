dynvariables:
  cpu_manufacturer: grep -i "model name" -m 1 /proc/cpuinfo | cut -d ':' -f 2 | cut -d ' ' -f 2
  cwd: pwd

variables:
  dunst_monitor: 0

  font: Iosevka Term Extended
  font_bold: Iosevka Term Bold Extended

  git_user_name: Krapaince
  git_user_email: Krapaince@pm.me
  git_user_signingkey: EF86373CA45DE4BC
  git_commit_gpgsign: 'true'
  git_epitech: true

  npm_default_dir: "{{@@ env['HOME'] @@}}/.npm-global"

  user: krapaince

  wallpaper_destination: "{{@@ env['HOME'] @@}}/Pictures/wallpapers"
  wallpaper_filename: avatar.png
  wallpaper_script: "python {{@@ env['HOME'] @@}}/.local/bin/wallpaper.py"
  wallpaper_color_shifter: true

 # Waybar / polybar network interfaces
  wired_network_interface:
  wireless_network_interface:
